# UI 目录结构

- `base/`: Activity、Fragment 基类和通用页面执行框架。
- `common/`: 跨页面复用的 UI 辅助能力，例如弹窗、触感反馈、跳转动画。
- `main/`: 首页及其控制器、列表、分组切换等主流程 UI。
- `settings/`: 设置、订阅、路由、资源、备份、更新等配置类页面。
- `server/`: 节点/服务器配置编辑相关页面。
- `tools/`: 日志、扫码、Tasker、UrlScheme 等工具型页面。

保留在 `ui/` 根目录的文件应尽量满足“跨子目录复用且不属于单一业务流”。
